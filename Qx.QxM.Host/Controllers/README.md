# README: Controllers

## Overview
API Controllers are used to set up the endpoints.

## Adding New Controllers
To add a new controller, right-click on the `Controllers` folder
in the `Qx.QxM.Host` project and select `Add` > `Scaffolded Item...` >
`Api Controller - Empty` then name it (e.g. `InventoryController`).